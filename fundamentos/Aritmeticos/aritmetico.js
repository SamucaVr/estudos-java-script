// Operador aritmético é um operador binário, pois precisa de dois operando

const [a, b, c, d] = [3, 5, 1, 15] // criando as variáveis usando o destructuring
const soma = a + b + c + d
const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2

console.log(soma, subtracao, multiplicacao, divisao, modulo)